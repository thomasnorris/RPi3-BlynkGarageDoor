
var _outerFunc = module.exports = {
	GetPaths: function() {
		// --Add new file paths here
		// --Reference them with require('local/paths').GetModule(name-of-file.js)
		
		var prepend = '../../bin/';
		return {
			'blynk-setup': prepend + 'Blynk/blynk-setup.js',
			'blynk-auth': prepend + 'Blynk/blynk-auth.js',
			'database-operations': prepend + 'Operations/database-operations.js',
			'date-time-operations': prepend + 'Operations/date-time-operations.js',
			'mapping': prepend + 'mapping.js'
		}
	},

	GetModule: function(name) {
		var paths = _outerFunc.GetPaths();
		return require(paths[name]);
	}
}